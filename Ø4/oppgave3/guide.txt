## To build the library and copy locally run:
    $ ./create_dynamic.sh

## Compile the project:
    $ ./compile_link.sh

## To rebuild library on code-change, rerun:
    $ ./compile_link.sh

## On fixed typo in more_functions and rebuilding library, the output has the corrected word.